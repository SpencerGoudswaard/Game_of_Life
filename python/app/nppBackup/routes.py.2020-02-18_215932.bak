from flask import render\_template()
from app import app

@app.route('/')
@app.route('/index')
def index():
    return "Hello, World"
    # user = {'username': 'Spencer'}
    # return render_template('Questlog.html', title='Home', user=user)
